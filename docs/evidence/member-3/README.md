# Member 3 evidence

Store generated Week 2 validation evidence here without committing credentials or environment-specific secrets.

- `k6-smoke-summary.json` is generated by `make k6-smoke` and is ignored by Git.
- Keep dashboard exports, API samples, and deployment output in subfolders once the shared environment is available.
- Record the branch, image tag, command, date, and scenario with every retained result.
